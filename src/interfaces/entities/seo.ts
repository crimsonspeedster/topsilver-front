export type SeoObject = {
    title: string,
    description: string|null,
    keywords: string|null,
    robots: string,
}

export type SeoBlockObject = {
    title: string,
    excerpt: string|null,
    content: string|null,
}